# Frontend dependencies (Node.js + React + Vite)
# NOTE: This is a human-readable summary. The real manifest is package.json.
# Install everything with:  npm install
#
# Runtime dependencies:
react==^19.2.6          # UI library
react-dom==^19.2.6      # React DOM renderer
#
# Dev dependencies:
vite==^8.0.12                       # Build tool / dev server
@vitejs/plugin-react==^6.0.1        # React support for Vite
eslint==^10.3.0                     # Linter
@eslint/js==^10.0.1
eslint-plugin-react-hooks==^7.1.1
eslint-plugin-react-refresh==^0.5.2
globals==^17.6.0
@types/react==^19.2.14
@types/react-dom==^19.2.3
