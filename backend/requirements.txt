# Backend dependencies (Node.js)
# NOTE: This is a human-readable summary. The real manifest is package.json.
# Install everything with:  npm install
#
# Runtime dependencies:
express==^5.2.1     # Web server / routing
cors==^2.8.6        # Enable cross-origin requests from the frontend
dotenv==^17.4.2     # Load environment variables from .env
#
# Dev dependencies:
nodemon==^3.1.14    # Auto-restart server on file changes (npm run dev)
